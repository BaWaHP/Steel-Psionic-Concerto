WUN_ZHE_war_intensify = {
	allowed = { OR = { tag = WUN tag = ZHE } }
	enable = {
		OR = { tag = WUN tag = ZHE }
		OR = { has_war_with = WUN has_war_with = ZHE }
	}
	abort = { OR = { WUN = { exists = no } ZHE = { exists = no } } }

	ai_strategy = {
        type = force_build_armies
        value = 100
    }

	ai_strategy = {
        type = ignore_army_incompetence
        value = 100
    }

	ai_strategy = {
		type = front_unit_request
		country_trigger = { OR = { tag = ZHE tag = WUN } }
		value = 100
	}

	ai_strategy = {
		type = front_control
		country_trigger = { OR = { tag = ZHE tag = WUN } }
		ratio = 0.6
		priority = 100
		ordertype = front
		execution_type = rush
	}
}

