SPC_generic_sekeleton_decision_category = {
	NOH_start_civil_war = {		#雀鸟内战
		icon = GFX_decision_eng_trade_unions_support
		visible = { tag = NOH }

		available = {
			date > 1999.6.1
		}

		fire_only_once = yes

		complete_effect = {
			news_event = NOHNews.1
			declare_war_on = {
				target = OHL
				type = annex_everything
			}
			declare_war_on = {
				target = OLA
				type = annex_everything
			}
			OLA = {
				declare_war_on = {
					target = OHL
					type = annex_everything
				}
			}
		}

		ai_will_do = {
			base = 50
		}
	}
	NOH_integrate_WCH = {		#整合乌骓
		icon = GFX_decision_eng_trade_unions_support
		visible = { SPC_generic_region_trigger_251 = yes }

		available = {
			WCH = { exists = no }
			owns_state = 296
			owns_state = 297
			owns_state = 299
			owns_state = 300
		}

		fire_only_once = yes

		complete_effect = {
			WCH = { add_to_ROOT_potential_cores = yes }
		}

		ai_will_do = {
			base = 50
		}
	}
}

SPC_generic_national_reunification_decision_category = {
	NOH_reunification_decision = {		#雀鸟统一决议
		icon = GFX_decision_eng_trade_unions_support
		visible = { SPC_generic_region_trigger_251 = yes }

		available = {
			SPC_generic_region_trigger_251_control = yes
		}

		fire_only_once = yes
		days_remove = 10
	
		remove_effect = {
			add_stability = 0.1
			add_war_support = 0.1
			add_political_power = 200
			for_each_scope_loop = {
				array = global.SPC_generic_region_states_251
				if = {
					limit = { NOT = { is_core_of = ROOT } }
					add_core_of = ROOT
				}
			}
			set_global_flag = SPC_region_unified_global_251
			if = {
				limit = { tag = NOH }
				news_event = NOHNews.2
				news_event = {
					id = OASNews.3
					days = 7
				}
				OAS = { 
					add_to_faction = NOH 
					OAS_weak_republic_down = yes
					OAS_nothing_but_brace_down = yes
				}
			}
			if = {
				limit = { tag = OLA }
				news_event = NOHNews.3
			}
			if = {
				limit = { tag = OHL }
				news_event = NOHNews.4
			}
		}

		ai_will_do = {
			base = 999
		}
	}
}

