on_actions = {
	on_declare_war = {
		###FROM - 目标###
		###ROOT - 攻击方###
		effect = {
			###西莉迪亚内战###
			if = {
				limit = {
					OR = {
						AND = {
							FROM = { tag = CLD }
							ROOT = { tag = NCD }		
						}
						AND = {
							FROM = { tag = NCD }
							ROOT = { tag = CLD }
						}
					}
				}
				news_event = CLDNews.2
			}
		}
	}
	on_daily_CLD = {
		effect = {
			if = {
				limit = { has_dynamic_modifier = CLD_red_army }
				CLD_red_army_update = yes
			}
		}
	}
}
